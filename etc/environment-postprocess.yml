---

name: pySDC
channels:
  - conda-forge
dependencies:
  # Pinned: see etc/environment-tests.yml for rationale (coverage>=7.15.1
  # intermittently SIGTERMs subprocess-spawning test jobs on CI).
  - coverage[toml]=7.15.0
  - sphinx
  - numpy
  - scipy
  - matplotlib
  - dill
  - numba
  - cupy
  - pip
  - pip:
    - sphinxemoji
    - m2r2
    - genbadge[all]
    - qmat>=0.1.8
