name: CI pipeline for pySDC

on:
  push:
  pull_request:

jobs:

  lint:
    runs-on: ubuntu-latest

    env:
      YML: 'etc/environment-lint.yml'

    defaults:
      run:
        shell: bash -l {0}

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Install Conda environment with Micromamba
        uses: mamba-org/provision-with-micromamba@main
        with:
          environment-file: ${{ env.YML }}

      - name: Code reformatting with black
        run: |
          black pySDC  --check --diff --color 

      - name: Linting with flakeheaven
        run: |
          flakeheaven lint --benchmark pySDC          


  test_base:
    runs-on: ubuntu-latest

    needs: lint

    env:
      YML: 'etc/environment-base.yml'

    defaults:
      run:
        shell: bash -l {0}

    strategy:
      matrix:
        py: [ "3.7", "3.8", "3.9", "3.10" ]
    
    steps:
      - name: Checkout
        uses: actions/checkout@v1
        
      - name: Add packages
        run: |
          sudo apt-get update
          sudo apt-get --yes install texlive-latex-recommended texlive-fonts-recommended texlive-latex-extra cm-super dvipng nodejs

      - name: Install Conda environment with Micromamba
        uses: mamba-org/provision-with-micromamba@main
        with:
          environment-file: ${{ env.YML }}
          extra-specs: python = ${{ matrix.py }}

      - name: Run pytest
        run: |
          coverage run --data-file=coverage_${{ github.job }}.dat -m pytest --continue-on-collection-errors -v --durations=0 pySDC/tests -m "not fenics and not petsc and not mpi"

      - name: Store results
        if: matrix.py == '3.10'
        uses: actions/upload-artifact@v3
        with:
          name: data_${{ github.job }}
          path: data

      - name: Store coverage results
        if: matrix.py == '3.10'
        uses: actions/upload-artifact@v3
        with:
          name: coverage_${{ github.job }}
          path: ./coverage_${{ github.job }}.dat


  test_fenics:
    runs-on: ubuntu-latest

    needs: lint

    env:
      YML: 'etc/environment-fenics.yml'

    defaults:
      run:
        shell: bash -l {0}

    strategy:
      matrix:
        py: [ "3.7", "3.8", "3.9", "3.10" ]

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Add packages
        run: |
          sudo apt-get update
          sudo apt-get --yes install texlive-latex-recommended texlive-fonts-recommended texlive-latex-extra cm-super dvipng nodejs

      - name: Install Conda environment with Micromamba
        uses: mamba-org/provision-with-micromamba@main
        with:
          environment-file: ${{ env.YML }}
          extra-specs: python = ${{ matrix.py }}

      - name: Run pytest
        run: |
          coverage run --data-file=coverage_${{ github.job }}.dat -m pytest --continue-on-collection-errors -v --durations=0 pySDC/tests -m fenics

      - name: Store results
        if: matrix.py == '3.10'
        uses: actions/upload-artifact@v3
        with:
          name: data_${{ github.job }}
          path: data

      - name: Store coverage results
        if: matrix.py == '3.10'
        uses: actions/upload-artifact@v2
        with:
          name: coverage_${{ github.job }}
          path: ./coverage_${{ github.job }}.dat


  test_petsc:
    runs-on: ubuntu-latest

    needs: lint

    env:
      YML: 'etc/environment-petsc.yml'

    defaults:
      run:
        shell: bash -l {0}

    strategy:
      matrix:
        py: [ "3.7", "3.8", "3.9", "3.10" ]

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Add packages
        run: |
          sudo apt-get update
          sudo apt-get --yes install texlive-latex-recommended texlive-fonts-recommended texlive-latex-extra cm-super dvipng nodejs

      - name: Install Conda environment with Micromamba
        uses: mamba-org/provision-with-micromamba@main
        with:
          environment-file: ${{ env.YML }}
          extra-specs: python = ${{ matrix.py }}

      - name: Run pytest
        run: |
          coverage run --data-file=coverage_${{ github.job }}.dat -m pytest --continue-on-collection-errors -v --durations=0 pySDC/tests -m petsc

      - name: Store results
        if: matrix.py == '3.10'
        uses: actions/upload-artifact@v3
        with:
          name: data_${{ github.job }}
          path: data

      - name: Store coverage results
        if: matrix.py == '3.10'
        uses: actions/upload-artifact@v2
        with:
          name: coverage_${{ github.job }}
          path: ./coverage_${{ github.job }}.dat


  test_mpi:
    runs-on: ubuntu-latest

    needs: lint

    env:
      YML: 'etc/environment-mpi4py.yml'

    defaults:
      run:
        shell: bash -l {0}

    strategy:
      matrix:
        py: [ "3.7", "3.8", "3.9", "3.10" ]

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Add packages
        run: |
          sudo apt-get update
          sudo apt-get --yes install texlive-latex-recommended texlive-fonts-recommended texlive-latex-extra cm-super dvipng nodejs

      - name: Install Conda environment with Micromamba
        uses: mamba-org/provision-with-micromamba@main
        with:
          environment-file: ${{ env.YML }}
          extra-specs: python = ${{ matrix.py }}

      - name: Run pytest
        run: |
          coverage run --data-file=coverage_${{ github.job }}.dat -m pytest --continue-on-collection-errors -v --durations=0 pySDC/tests -m mpi

      - name: Store results
        if: matrix.py == '3.10'
        uses: actions/upload-artifact@v3
        with:
          name: data_${{ github.job }}
          path: data

      - name: Store coverage results
        if: matrix.py == '3.10'
        uses: actions/upload-artifact@v2
        with:
          name: coverage_${{ github.job }}
          path: ./coverage_${{ github.job }}.dat


  coverage_report:
    runs-on: ubuntu-latest

    needs:
      - test_base
      - test_fenics
      - test_petsc
      - test_mpi

    steps:
      - name: Checkout
        uses: actions/checkout@v1

#      - name: Creating codecov.yml
#        run: echo 'fixes:\n  - "::../"' > codecov.yml

      - name: Install coverage.py
        run: |
          python -m pip install --upgrade coverage[toml]

      - name: Get coverage reports
        uses: actions/download-artifact@v3
        with:
          path: coverage

      - name: Combine coverage reports
        run: |
          ls -ratl coverage
          rm -rf coverage/data_*
          find coverage -type f -print0 | xargs -0 -I file mv --backup=numbered file coverage
          ls -ratl coverage
          python -m coverage combine ./coverage

      - name: Upload coverage reports
        uses: codecov/codecov-action@v3
        with:
          directory: coverage
          verbose: true # optional (default = false)
          name: codecov-pySDC # optional
          fail_ci_if_error: false # optional (default = false)


  doc_and_deploy:
    runs-on: ubuntu-latest

    needs:
      - test_base
      - test_fenics
      - test_petsc
      - test_mpi

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Get data
        uses: actions/download-artifact@v3
        with:
          path: data

      - name: Check files
        run: |
          rm -rf data/coverage_* data/README
          find data -type f -print0 | xargs -0 -I file mv --backup=numbered file data
          rm -rf data/README*
          ls -artl data

      - name: Install Sphinx
        run: python -m pip install --upgrade sphinx

      - name: Build documentation
        run: |
          chmod u+x ./docs/update_apidocs.sh
          ./docs/update_apidocs.sh
          sphinx-build -b html docs/source docs/build/html

#      - name: Deploy 🚀
#        uses: JamesIves/github-pages-deploy-action@4.1.4
#        if: ${{ github.event_name == 'push' && github.repository_owner == 'Parallel-in-Time' && github.ref == 'refs/heads/master' && !env.ACT }}
#        with:
#          branch: gh-pages # The branch the action should deploy to.
#          folder: docs/build/html # The folder the action should deploy.